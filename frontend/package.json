{
  "name": "notes-frontend",
  "version": "1.0.0",
  "description": "Frontend для приложения заметок",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3001",
    "dev": "live-server --port=3001",
    "build": "echo 'Frontend build completed'",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "format": "prettier --write 'src/**/*.{js,html,css}'"
  },
  "devDependencies": {
    "eslint": "^8.53.0",
    "prettier": "^3.0.3",
    "live-server": "^1.2.2"
  }
}